package equipamentos.digitalizadoras;

// Trocamos a palavras "class" por "interface" para cria-lá.
public interface Digitalizadora {
    // Apenas refefimos o métodos que os filhos precisam ter.
    public void ditalizar();
}
